services:
  - type: web
    name: zstradio-landing
    runtime: node
    region: frankfurt
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: HOST
        value: 0.0.0.0
